# compact

[INTENT: REFERENCE]

`compact` takes a list of strings and returns a new list with any empty string elements removed.

## Signature

```hcl
compact(list)
```

## Parameters

- `list` (`list(string)`)

## Return type

`list(string)`

Official documentation: [compact](https://opentofu.org/docs/language/functions/compact/)
