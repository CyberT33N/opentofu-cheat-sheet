# trimprefix

[INTENT: REFERENCE]

`trimprefix` removes the specified prefix from the start of the given string. If the string does not start with the prefix, the string is returned unchanged.

## Signature

```hcl
trimprefix(str, prefix)
```

## Parameters

- `str` (`string`)
- `prefix` (`string`)

## Return type

`string`

Official documentation: [trimprefix](https://opentofu.org/docs/language/functions/trimprefix/)
