# sort

[INTENT: REFERENCE]

`sort` takes a list of strings and returns a new list with those strings sorted lexicographically.

## Signature

```hcl
sort(list)
```

## Parameters

- `list` (`list(string)`)

## Return type

`list(string)`

Official documentation: [sort](https://opentofu.org/docs/language/functions/sort/)
