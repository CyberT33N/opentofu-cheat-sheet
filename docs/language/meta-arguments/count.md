# count

[INTENT: REFERENCE]

The `count` meta-argument creates the given number of instances of a resource or module, addressable as `<addr>[<index>]` with `count.index` available inside the block. The official language tree carries this topic as a page of the [meta-arguments](overview.md) area.

## Official documentation

- [count](https://opentofu.org/docs/language/meta-arguments/count/)
