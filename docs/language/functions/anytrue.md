# anytrue

[INTENT: REFERENCE]

`anytrue` returns `true` if any element in a given collection is `true` or `"true"`. It also returns `false` if the collection is empty.

## Signature

```hcl
anytrue(list)
```

## Parameters

- `list` (`list(bool)`)

## Return type

`bool`

Official documentation: [anytrue](https://opentofu.org/docs/language/functions/anytrue/)
