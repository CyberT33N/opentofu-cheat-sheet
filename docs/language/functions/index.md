# index

[INTENT: REFERENCE]

`index` finds the element index for a given value in a list.

## Signature

```hcl
index(list, value)
```

## Parameters

- `list` (`dynamic`)
- `value` (`dynamic`)

## Return type

`dynamic`

Official documentation: [index](https://opentofu.org/docs/language/functions/index_function/) — the official page slug is `index_function`, because `index` is reserved for the section index.

