# regex

[INTENT: REFERENCE]

`regex` applies a regular expression to a string and returns the matching substrings.

## Signature

```hcl
regex(pattern, string)
```

## Parameters

- `pattern` (`string`)
- `string` (`string`)

## Return type

`dynamic`

Official documentation: [regex](https://opentofu.org/docs/language/functions/regex/)
