# keys

[INTENT: REFERENCE]

`keys` takes a map and returns a list containing the keys from that map.

## Signature

```hcl
keys(inputMap)
```

## Parameters

- `inputMap` (`dynamic`) — the map to extract keys from; may instead be an object-typed value, in which case the result is a tuple of the object attributes.

## Return type

`dynamic`

Official documentation: [keys](https://opentofu.org/docs/language/functions/keys/)
